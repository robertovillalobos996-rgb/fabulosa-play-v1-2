export const initTV = () => { window.addEventListener("keydown", (e) => { const items = document.querySelectorAll("button, [tabindex=\"0\"], a"); let idx = Array.from(items).indexOf(document.activeElement); if (e.key === "ArrowRight") items[idx + 1]?.focus(); if (e.key === "ArrowLeft") items[idx - 1]?.focus(); if (e.key === "ArrowDown") items[idx + 4]?.focus(); if (e.key === "ArrowUp") items[idx - 4]?.focus(); if (e.key === "Enter") document.activeElement.click(); }); };
